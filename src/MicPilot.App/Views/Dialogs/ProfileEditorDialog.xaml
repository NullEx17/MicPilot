<Window x:Class="MicPilot.App.Views.Dialogs.ProfileEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:MicPilot.App.Controls"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Add Game or App"
        Width="420"
        Height="520"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}"
        Foreground="{StaticResource TextBrush}"
        FontFamily="Segoe UI Variable Display, Segoe UI"
        ResizeMode="NoResize"
        WindowStyle="None"
        ShowInTaskbar="False">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="42"
                            ResizeBorderThickness="0"
                            GlassFrameThickness="0"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Background="{StaticResource BackgroundBrush}">
        <DockPanel>
            <Border DockPanel.Dock="Top"
                    Height="42"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,0,0,1">
                <Grid Margin="10,0">
                    <Button Style="{StaticResource CloseWindowControlStyle}"
                            HorizontalAlignment="Left"
                            Click="Cancel_Click"
                            shell:WindowChrome.IsHitTestVisibleInChrome="True" />
                    <TextBlock x:Name="TitleText"
                               Text="Add Game or App"
                               FontWeight="SemiBold"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
                </Grid>
            </Border>

            <Border DockPanel.Dock="Bottom" Padding="20,14" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0" Click="Cancel_Click" />
                    <Button x:Name="ConfirmButton" Content="Add" Style="{StaticResource PrimaryButtonStyle}" MinWidth="80" Click="Confirm_Click" />
                </StackPanel>
            </Border>

            <ScrollViewer Padding="20,16">
                <StackPanel>
                    <TextBlock Text="Name" Style="{StaticResource MutedTextStyle}" Margin="0,0,0,6" />
                    <TextBox x:Name="NameBox" Style="{StaticResource TextBoxDarkStyle}" Margin="0,0,0,14" />

                    <TextBlock Text="Application" Style="{StaticResource MutedTextStyle}" Margin="0,0,0,6" />
                    <Grid Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="8" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="ProcessBox" Style="{StaticResource TextBoxDarkStyle}" />
                        <Button Grid.Column="2" Content="Browse..." Style="{StaticResource SecondaryButtonStyle}" Click="Browse_Click" />
                    </Grid>

                    <TextBlock Text="Hotkey" Style="{StaticResource MutedTextStyle}" Margin="0,0,0,6" />
                    <controls:HotkeyCaptureBox x:Name="HotkeyBox" Margin="0,0,0,14" />

                    <TextBlock Text="Mode" Style="{StaticResource MutedTextStyle}" Margin="0,0,0,8" />
                    <RadioButton x:Name="ToggleModeBox"
                                 GroupName="Mode"
                                 Content="Toggle  —  Press to mute / unmute"
                                 IsChecked="True"
                                 Foreground="{StaticResource TextBrush}"
                                 Margin="0,0,0,8" />
                    <RadioButton x:Name="WalkieModeBox"
                                 GroupName="Mode"
                                 Content="Walkie-talkie  —  Hold to talk"
                                 Foreground="{StaticResource TextBrush}"
                                 Margin="0,0,0,16" />

                    <Grid Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="Auto activate" FontWeight="SemiBold" />
                            <TextBlock Text="Switch to this profile when the app starts."
                                       Style="{StaticResource DimTextStyle}"
                                       Margin="0,3,0,0" />
                        </StackPanel>
                        <CheckBox x:Name="AutoActivateBox"
                                  Style="{StaticResource ToggleSwitchStyle}"
                                  HorizontalAlignment="Right"
                                  VerticalAlignment="Center" />
                    </Grid>

                    <Grid>
                        <StackPanel>
                            <TextBlock Text="Enabled" FontWeight="SemiBold" />
                            <TextBlock Text="Include this profile in MicPilot."
                                       Style="{StaticResource DimTextStyle}"
                                       Margin="0,3,0,0" />
                        </StackPanel>
                        <CheckBox x:Name="EnabledBox"
                                  Style="{StaticResource ToggleSwitchStyle}"
                                  HorizontalAlignment="Right"
                                  VerticalAlignment="Center"
                                  IsChecked="True" />
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </DockPanel>
    </Border>
</Window>
